# 2026-08-30

## kkFileView-jdk1.8：sevenzipjbinding 按架构分包改造
- 问题：`server/pom.xml` 原把 `sevenzipjbinding` 核心 + `all-platforms` + `linux-arm64` + `mac-x86_64` 四个 jar 一起打进包。`all-platforms` 不含 arm64/mac-x86_64 的 native 库，同包后运行到对应架构时 sevenzipjbinding 会错误加载 all-platforms 同名 native 库，须手动删 jar。
- 方案：把平台 native 库移入 Maven `<profiles>`：`sevenzip-all`(activeByDefault，amd64)、`sevenzip-linux-arm64`、`sevenzip-mac-x86_64`。显式 `-P` 激活某 profile 时默认 profile 自动失效，故每个包只含对应 jar。
- 工作流：`.github/workflows/docker-image.yml` 与 `maven-publish.yml` 改为 matrix 按架构构建并产出 `dist/kkFileView-<arch>.tar.gz`；`Dockerfile` 用 `ARG TARGETARCH` + `ADD dist/kkFileView-${TARGETARCH}.tar.gz` 选对应产物，arm64 容器不再冲突。
- 本地构建：`mvn clean package -Psevenzip-linux-arm64`（arm64）/ `-Psevenzip-all`（amd64 默认）/ `-Psevenzip-mac-x86_64`。
- 注：仓库是从 https://github.com/jiangchuanso/kkFileView-jdk1.8 浅克隆到工作区的。

## kkFileView-jdk1.8：上游(kekingcn)提交可合入性分析
- 已添加远程 `keking`(kekingcn/kkFileView) 与 `upstream`(xxs3315/kkFileView， preexisting)。
- 分叉点：本分支(origin/master) 与 keking/master 的 merge-base = `6746325`（v4.4.0，jdk1.8）。
- 上游自分叉点起共 116 个提交（到 v5.0.2）。**关键升级边界提交 `f6c6e22b`**：JDK1.8→JDK21、spring-boot 2.4.2→3.5.6（#687）。其后还有 `e44ef813`(Dockerfile→JDK21)、`92ca92be`(支持Java25)、`e1e146cb`(因jdk升级修tif) —— 这 4 个「JDK升级族」**禁止合入**（正是要保留 jdk1.8 所排除的）。
- 可直接合入的 Java 提交（升级前、jdk1.8 时代，0 处 jakarta）：`fdb40680`(禁用上传接口#656)、`6cdbf92f`(上传禁用体验#684)、`a9787b0a`(JSON预览#685)、`51653483`(Kl json#686) —— 共 4 个。
- 升级后的 37 个 Java 提交：使用 jakarta/Java17+，不可直接 cherry-pick，需手工移植(javax、spring-boot 2.x、Java8 语法)。
- 71 个非 Java 提交（docs/CI/前端/docker/release）：前端类(OFD表格溢出、LuckyExcel matchType崩溃#730、水印脚本、pdf跨域、反代路径)可评估 cherry-pick；Dockerfile JDK21、版本号 bump 到 5.0.x、CI auto-deploy、e2e 测试套件需谨慎/跳过。
- 结论：保留 jdk1.8 时，能直接拿的只有升级前的 4 个 Java 提交 + 不依赖 jdk 的前端/文档修复；5.0.0 大量新功能因基于 JDK21 需逐个手工移植。

## 合入取舍（用户最终决定）
- 安全限制类 **不要**：`fdb40680`(#656 禁用上传接口)、`6cdbf92f`(#684 上传禁用体验)。
- 预览能力类 **要**：`a9787b0a`(#685 JSON 格式化预览)、`51653483`(#686 Kl json)。
- 已将这两个预览提交 cherry-pick 到新分支 `feat/upstream-json-preview`（于 16f38c3 之上），均自动合并无冲突；已核对 FileType.JSON / FilePreview 注册 / JsonFilePreviewImpl 引用一致。
- 工作流：`.github/workflows/maven-publish.yml` 增加 `tags: ['v*']` 并在 `v*` tag 推送时通过 `softprops/action-gh-release@v2` 发布 GitHub Release，附件为各架构 `BUILD_ARTIFACT-<arch>` 的 tar.gz/zip。
- 尚未 push/创建 PR（按约定不擅自推送）；等待用户确认后合并到 master。
